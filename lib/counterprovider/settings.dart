import 'package:flutter/material.dart';

class CounterSettings extends StatelessWidget {
  const CounterSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
      body: Center(
        child: Text('home'),
      ),
    );
  }
}
